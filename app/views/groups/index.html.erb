<nav class="navbar">
</nav>
<p style="color: green"><%= notice %></p>

<h1>Groups</h1>

<div id="groups">
  <% @groups.each do |group| %>
    <%= render group %>
  <% end %>
</div>

<%= button_to "New group", new_group_path, method: :get %>
